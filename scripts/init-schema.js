/*
  SQLite schema init script (deprecated).

  This app now uses Postgres via Prisma.

  Use one of:
    npx prisma migrate dev
    npx prisma db push
*/

console.log('ℹ️  Deprecated: use Prisma migrations (Postgres) instead of init-schema.js')
process.exit(0)
